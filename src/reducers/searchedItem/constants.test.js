import * as constants from "./constants";

describe("constants", () => {
  it("should have the action SET_SEARCH_ITEM_NAME", () => {
    expect(constants.SET_SEARCH_ITEM_NAME).toEqual("SET_SEARCH_ITEM_NAME");
  });
});
